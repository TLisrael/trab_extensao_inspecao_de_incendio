package com.engenharia.inspecao.ui.theme

import androidx.compose.ui.graphics.Color

// Cores do tema - Branco e Vermelho conforme solicitado

// Cores Primárias (Vermelho)
val Vermelho = Color(0xFFD32F2F) // Vermelho principal
val VermelhoEscuro = Color(0xFFB71C1C) // Vermelho mais escuro
val VermelhoClaro = Color(0xFFEF5350) // Vermelho mais claro

// Cores de Fundo e Superfície (Branco)
val Branco = Color(0xFFFFFFFF)
val CinzaClaro = Color(0xFFF5F5F5) // Fundo alternativo
val Cinza = Color(0xFFE0E0E0) // Bordas e divisores

// Cores de Texto
val TextoEscuro = Color(0xFF212121) // Texto principal
val TextoMedio = Color(0xFF757575) // Texto secundário
val TextoClaro = Color(0xFFBDBDBD) // Texto desabilitado
